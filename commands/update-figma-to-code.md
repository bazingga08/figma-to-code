Run the figma-to-code plugin updater. This pulls latest from GitHub, syncs to the local cache, and verifies everything is up to date.

Execute this command:
```bash
bash ~/.claude/plugins/marketplaces/figma-to-code/UPDATE.sh
```

After the script completes, tell the user to run `/reload-plugins` to load the updated skill into the current session.
